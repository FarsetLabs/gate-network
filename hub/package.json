{
  "name": "central-command",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node ./index.js",
    "test": "node ./tests/interaction.test.js",
    "format": "npm run format:js && npm run format:json && npm run format:md",
    "format:js": "prettier './**/*.js' --write",
    "format:json": "prettier './**/*.json' --write",
    "format:md": "prettier './**/*.md' --write --prose-wrap=\"always\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.2",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "prettier": "^3.3.2"
  }
}
